{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/preinstall.mjs",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:vercel": "cross-env PORT=5173 BASE_PATH=/ pnpm --filter @workspace/primeaxis run build && pnpm --filter @workspace/api-server run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:site": "cross-env PORT=5173 BASE_PATH=/ API_PROXY_TARGET=http://localhost:5000 pnpm --filter @workspace/primeaxis run dev",
    "dev:studio": "cross-env PORT=3333 pnpm --filter primeaxis-studio run dev"
  },
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.2",
    "prettier": "^3.8.4",
    "serverless-http": "^3.2.0",
    "typescript": "~6.0.3"
  }
}
